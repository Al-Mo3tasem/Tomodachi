# Ignore local scratch config outside the app module
/config.js

# Never commit seed page after use
seed.html

docs/Tomodachi_Master_Plan.md
docs/Commercialization_Plan.md

# T4 content per PROJECT_RULES.md §18.2 — never commit authoring artifacts
# (content drafts, prompts, intermediate JSON, generated audio caches)
/content/
/scripts/cache/
/scripts/output/
/scripts/prompts/
audio/
*.mp3
*.wav
*.ogg

# L2.04 authoring CLI — local-only state + secrets (never commit)
/scripts/node_modules/
/scripts/progress/
/scripts/secrets/
/scripts/tmp/

# Generated image assets (L2 image pipeline). _candidates are throwaway rejected
# attempts; finals get reviewed then hosted (Firebase Storage / CDN). Don't let a
# stray `git add .` stage ~1GB of PNGs — force-add specific finals if ever needed.
/assets/generated/

# Obsidian editor config (personal, any vault location)
.obsidian/
docs/.obsidian/

# Claude Code personal settings (machine-local permissions)
.claude/settings.local.json

# Native shell (Capacitor). www/ is generated by scripts/native/build-www.mjs;
# android/ and ios/ project files ARE committed, their build outputs are not.
/node_modules/
/www/
/dist/
/android/app/build/
/android/build/
/android/.gradle/
/android/.idea/
/android/local.properties
/android/app/release/
/ios/App/build/
/ios/App/Pods/
/ios/App/App.xcworkspace/xcuserdata/
*.keystore
*.jks
keystore.properties
/android/app/src/main/assets/public/
/ios/App/App/public/
/tests/e2e/.results/
/tests/e2e/.report/
/shots/
